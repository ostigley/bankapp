%h1
  = params[:category]
  in 
  = params[:date]

- @transactions.each do |day, tx_array|
  - next if tx_array.empty?
  %h3
    = day
  - tx_array.each do |tx|
    %p
      = "id: #{tx.id}"
    = link_to tx.detail, "/transaction-detail/#{tx.detail}"
    %p
      = tx.amount
  %hr